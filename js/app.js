$(document).foundation();

//CONFIGURATORE
var $obj3D = $('#obj-canvas');
var $window = $(window);

var w_window = $window.width();
var h_window = $window.height();
$obj3D.width(w_window).height(h_window);
